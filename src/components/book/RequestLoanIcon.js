import React from 'react';

const RequestLoanIcon = ({ onClick }) => {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      width="24"
      height="24"
      viewBox="0 0 24 24"
      onClick={onClick}
      aria-label="Solicitar Préstamo"
    >
      {/* Aquí va el código SVG de tu ícono */}
    </svg>
  );
};

export default RequestLoanIcon;
